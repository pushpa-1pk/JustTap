const { BOOKING_STATUS } = require('./booking-status');
const env = require('../config/env');

const BOOKING_TYPES = Object.freeze({
  INSTANT: 'INSTANT',
  SCHEDULED: 'SCHEDULED'
});

const TIMEOUT_CONFIGS = Object.freeze({
  PROVIDER_ACCEPT_TIMEOUT_MS: env.providerAcceptTimeoutMs,
  OTP_EXPIRY_MS: env.otpExpiryMs,
  DISPUTE_WINDOW_MAX_HOURS: env.disputeWindowMaxHours
});

// Structural Matrix Map for Valid State Paths
const STATE_TRANSITION_MATRIX = Object.freeze({
  [BOOKING_STATUS.REQUESTED]: [BOOKING_STATUS.SEARCHING_PROVIDER, BOOKING_STATUS.CANCELLED],
  [BOOKING_STATUS.SEARCHING_PROVIDER]: [BOOKING_STATUS.PENDING_PROVIDER_RESPONSE, BOOKING_STATUS.FAILED, BOOKING_STATUS.CANCELLED],
  [BOOKING_STATUS.PENDING_PROVIDER_RESPONSE]: [BOOKING_STATUS.PROVIDER_ACCEPTED, BOOKING_STATUS.SEARCHING_PROVIDER, BOOKING_STATUS.CANCELLED],
  [BOOKING_STATUS.PROVIDER_ACCEPTED]: [BOOKING_STATUS.ON_THE_WAY, BOOKING_STATUS.CANCELLED],
  [BOOKING_STATUS.ON_THE_WAY]: [BOOKING_STATUS.ARRIVED, BOOKING_STATUS.CANCELLED],
  [BOOKING_STATUS.ARRIVED]: [BOOKING_STATUS.SERVICE_STARTED, BOOKING_STATUS.CANCELLED],
  [BOOKING_STATUS.SERVICE_STARTED]: [BOOKING_STATUS.SERVICE_COMPLETED],
  [BOOKING_STATUS.SERVICE_COMPLETED]: [BOOKING_STATUS.PAYMENT_PENDING, BOOKING_STATUS.COMPLETED, BOOKING_STATUS.DISPUTED],
  [BOOKING_STATUS.PAYMENT_PENDING]: [BOOKING_STATUS.COMPLETED, BOOKING_STATUS.DISPUTED, BOOKING_STATUS.FAILED],
  [BOOKING_STATUS.DISPUTED]: [BOOKING_STATUS.COMPLETED, BOOKING_STATUS.FAILED],
  [BOOKING_STATUS.COMPLETED]: [],
  [BOOKING_STATUS.CANCELLED]: [],
  [BOOKING_STATUS.FAILED]: []
});

module.exports = { BOOKING_TYPES, TIMEOUT_CONFIGS, STATE_TRANSITION_MATRIX };
