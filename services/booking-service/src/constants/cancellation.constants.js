const env = require('../config/env');

const CANCELLATION_REASON = Object.freeze({
  CUSTOMER_CHANGED_MIND: 'CUSTOMER_CHANGED_MIND',
  FOUND_BETTER_PRICE: 'FOUND_BETTER_PRICE',
  PROVIDER_DELAYED: 'PROVIDER_DELAYED',
  PROVIDER_NO_SHOW: 'PROVIDER_NO_SHOW',
  CUSTOMER_NO_SHOW: 'CUSTOMER_NO_SHOW',
  EMERGENCY: 'EMERGENCY',
  OTHER: 'OTHER'
});

const REFUND_STATUS = Object.freeze({
  NONE: 'NONE',
  PROCESSING: 'PROCESSING',
  COMPLETED: 'COMPLETED',
  FAILED: 'FAILED'
});

const CANCELLATION_POLICIES = Object.freeze({
  CUSTOMER_PENALTY_AFTER_ACCEPT: env.customerCancellationAfterAccept || 50,
  PROVIDER_PENALTY_ON_THE_WAY: env.providerCancellationOnTheWay || 100,
  CURRENCY: 'INR'
});

module.exports = { CANCELLATION_REASON, REFUND_STATUS, CANCELLATION_POLICIES };