const RESCHEDULE_LIMITS = Object.freeze({
  MAX_RESCHEDULES: 3,
  MIN_NOTICE_MINUTES: 30
});

const RESCHEDULE_REASON = Object.freeze({
  CUSTOMER_REQUEST: 'CUSTOMER_REQUEST',
  PROVIDER_REQUEST: 'PROVIDER_REQUEST',
  EMERGENCY: 'EMERGENCY',
  WEATHER: 'WEATHER',
  OTHER: 'OTHER'
});

const RESCHEDULE_STATUS = Object.freeze({
  PENDING: 'PENDING',
  ACCEPTED: 'ACCEPTED',
  REJECTED: 'REJECTED',
  EXPIRED: 'EXPIRED'
});

module.exports = {
  RESCHEDULE_LIMITS,
  RESCHEDULE_REASON,
  RESCHEDULE_STATUS
};